void print_data(char * name);

void set_admin_credentials(char * firstname, char * lastname, char * username, char * password, struct admin * admin);
void set_user_credentials(char * firstname, char * lastname, char * username, char * password, struct user * user);